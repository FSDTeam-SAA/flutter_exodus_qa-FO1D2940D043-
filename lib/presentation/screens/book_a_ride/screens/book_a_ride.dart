import 'package:flutter/material.dart';

class BookARideScreen extends StatelessWidget {
  const BookARideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Book a ride")));
  }
}
